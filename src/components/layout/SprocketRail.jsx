export default function SprocketRail() {
  return (
    <div
      className="w-3.5 shrink-0 border-r border-hairline bg-panel relative"
      style={{
        backgroundImage:
          "radial-gradient(circle, var(--color-ink) 2.5px, transparent 2.6px)",
        backgroundSize: "14px 20px",
        backgroundPosition: "0px 5px",
      }}
      aria-hidden="true"
    />
  );
}
